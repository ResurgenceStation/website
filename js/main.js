// Empty entrypoint; per-module scripts are loaded by the page directly.
// Kept around so the webpack copy plugin always has at least one js/ file
// and so future global wiring (e.g., theme toggle) has a single home.
